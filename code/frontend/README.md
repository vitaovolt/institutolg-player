# SPA — Instituto LG Biblioteca

React + Vite + Tailwind. Pastas Educraft (`api`, `pages`, `components/layout|ui`, …).

## Setup

```powershell
npm install
npm run dev
```

Proxy `/api` → `http://127.0.0.1:8000`. Health na home (F0).

Login, envio de aulas e player entram nas próximas fases.
